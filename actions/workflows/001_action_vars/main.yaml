---
version: 1.0
description: available vars 

input:
  - name

vars:
  - location: "never used"

output: 
  - myLocation: <% ctx().name %>

tasks:
  diplay_vars:
    action: core.echo
    input:
      message: <% ctx().name %> 
