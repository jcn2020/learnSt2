---
version: 1.0
description: available vars 

input:
  - name
  - location

vars:
  - notUsed: "never used"

output: 
  - myName: <% ctx().name %>
  - myLocation: <% ctx().location %>

tasks:
  diplay_vars:
    action: core.echo
    input:
      message: "<% ctx().name %>  and <% ctx().location %>"
    next:
      - when: <% succeeded() %>
        publish: notUsed="hello"

