---
version: 1.0
description: install package 

input: 
  - git_packs

vars:
  - error_code: 'nil'
  - pack_ref_name: 
      - "g_test"

output: 
  - mysource: <% ctx().git_packs %>
  - myErrCode: <% ctx().error_code %>

tasks:
  install_package:
    action: packs.uninstall
    input:  
      # packs: <% ctx().git_packs %>
      packs: <% ctx().pack_ref_name %>
    next:
      - when: <% succeeded() %> 
        publish:
          - error_code: '50'