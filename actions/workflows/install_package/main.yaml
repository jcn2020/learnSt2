---
version: 1.0
description: install package 

input: 
  - git_packs

vars:
  - error_code: 'nil'

output: 
  - mysource: <% ctx().git_packs %>
  - myErrCode: <% ctx().error_code %>

tasks:
  install_package:
    action: packs.uninstall
    input:  
      # packs: <% ctx().git_packs %>
      packs: "g_test"
    next:
      - when: <% succeeded() %> 
        publish:
          - error_code: '50'